<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ar.team.company.schoolsupplies">

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SchoolSupplies">
        <activity
            android:name=".ui.activitys.add.AddToolActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activitys.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activitys.sign.SignActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activitys.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>